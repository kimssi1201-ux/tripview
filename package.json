{
  "name": "tripview",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Data-driven Korean travel magazine with RSS, sitemap, and scheduled post update workflow.",
  "scripts": {
    "build": "node scripts/fix-invalid-regions.mjs && node scripts/render-valley-pages.mjs && node scripts/build-www.mjs",
    "test": "node --test tests/*.test.mjs",
    "lint": "node scripts/check-syntax.mjs",
    "audit:content": "node scripts/audit-content-quality.mjs",
    "enrich:short": "node scripts/enrich-short-posts.mjs",
    "refresh": "node scripts/update-posts.mjs && node scripts/fix-invalid-regions.mjs"
  }
}
